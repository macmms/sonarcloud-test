language: java
addons:
  sonarcloud:
    organization: "jakeuskoski" # the key of the org you chose at step #3
    token:
      secure: SONAR_TOKEN
script:
  - mvn clean verify sonar:sonar -Pcoverage -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=$SONAR_TOKEN -Dsonar.projectKey=JakeUskoski_testzone